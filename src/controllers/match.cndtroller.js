import { userPrisma, playerPrisma } from '../utils/prisma/index.js';

//전적 조회 API
export const recordsCheck = async (req, res, next) => {
    //경로 매개변수로 조회할 팀명을 받아온다
    const {userId} = req.params;

    //조회할 감독ㅓㄹ
  };
  